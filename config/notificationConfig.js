const notificationConfig = {
  statusEnum: ['sent', 'seen'],
  defaultStatus: 'sent',
  seenStatus: 'seen',
  notificationTypesEnum: ['follow', 'contact-request'],
  followNotificationType: 'follow',
  CRNotificationType: 'contact-request',
};

module.exports = notificationConfig;
